# Project Agent Rules

Use concise Chinese answers.

Before proposing implementation, inspect available project files.

Do not create a long questionnaire for fuzzy requests; give a default direction and ask only one material question if needed.

Do not claim browser or native question UI is available unless the active host tool list exposes it.
